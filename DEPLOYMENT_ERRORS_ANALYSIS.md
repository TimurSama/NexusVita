# Анализ ошибок деплоя - Детальный разбор

## 🔴 КРИТИЧЕСКИЕ ОШИБКИ (блокируют деплой)

### 1. **TypeError: Cannot read properties of undefined (reading 'map')** 
**Файл:** `app/(public)/demo/page.tsx:282`
**Проблема:** 
- `getValuePoints(perspective, lang)` возвращает `undefined` вместо массива
- Причина: `perspective` или `lang` имеют неправильные значения при статической генерации
- Влияние: Страница `/demo` не может быть сгенерирована статически

**Код:**
```typescript
{getValuePoints(perspective, lang).map((point: string, i: number) => (
```

**Решение:** Добавить проверку и fallback перед `.map()`

---

### 2. **Error: Unsupported Server Component type: {...}**
**Файлы:** 
- `app/(public)/demo/page.tsx`
- `app/(public)/presentation/page.tsx`

**Проблема:**
- Next.js пытается сериализовать данные из `components/presentation/data.tsx` для статической генерации
- В данных есть React компоненты (иконки) которые не могут быть сериализованы
- `export const dynamic = 'force-dynamic'` НЕ РАБОТАЕТ в клиентских компонентах (`'use client'`)

**Влияние:** 
- Страницы `/demo` и `/presentation` не могут быть статически сгенерированы
- Блокирует весь процесс сборки

**Решение:** 
- Убрать `export const dynamic` из клиентских компонентов (не работает)
- Использовать `generateStaticParams = () => []` или отключить статическую генерацию через `next.config.js`

---

### 3. **Dynamic server usage errors в API роутах**
**Файлы:**
- `app/api/achievements/user/route.ts` - использует `nextUrl.searchParams`
- `app/api/auth/me/route.ts` - использует `request.cookies`
- `app/api/calendar/ics/route.ts` - использует `nextUrl.searchParams`
- `app/api/integrations/ingestions/route.ts` - использует `nextUrl.searchParams`
- `app/api/oauth/garmin/callback/route.ts` - использует `nextUrl.searchParams`
- `app/api/oauth/oura/callback/route.ts` - использует `nextUrl.searchParams`
- `app/api/referrals/code/route.ts` - использует `nextUrl.searchParams`
- `app/api/specialists/route.ts` - использует `nextUrl.searchParams`

**Проблема:**
- Next.js пытается статически сгенерировать API роуты во время сборки
- Эти роуты используют динамические данные (`searchParams`, `cookies`)
- Нет `export const dynamic = 'force-dynamic'` в этих файлах

**Влияние:**
- Предупреждения во время сборки (не блокируют, но показывают проблемы)
- Могут вызвать проблемы в runtime

**Решение:** Добавить `export const dynamic = 'force-dynamic'` в каждый файл

---

## ⚠️ ПРЕДУПРЕЖДЕНИЯ (не блокируют, но показывают проблемы)

### 4. **ESLint warnings - неиспользуемые переменные**
**Файлы:** Множество файлов в `app/(dashboard)/` и `components/`

**Проблема:**
- Импортированные компоненты/переменные не используются
- `any` типы вместо конкретных типов
- Отсутствующие зависимости в React Hooks

**Влияние:** 
- Не блокируют деплой
- Ухудшают качество кода
- Могут скрывать реальные проблемы

---

### 5. **Edge Runtime warning**
**Файл:** `lib/auth/token.ts`
**Проблема:** Использует Node.js модуль `crypto` который не поддерживается в Edge Runtime

**Влияние:** Предупреждение, не блокирует деплой

---

## 📋 ПЛАН ИСПРАВЛЕНИЙ

### Приоритет 1 (критично):
1. ✅ Исправить `getValuePoints` - добавить проверку на undefined
2. ✅ Убрать `export const dynamic` из клиентских компонентов
3. ✅ Добавить `export const dynamic = 'force-dynamic'` во все API роуты с динамическими данными
4. ✅ Проверить `data.tsx` на несериализуемые данные

### Приоритет 2 (желательно):
5. Очистить неиспользуемые импорты
6. Заменить `any` на конкретные типы
7. Исправить зависимости в React Hooks
