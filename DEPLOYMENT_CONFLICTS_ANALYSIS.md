# 🔴 Анализ конфликтующих систем и причин ошибок

## КОРНЕВАЯ ПРОБЛЕМА

### Конфликт: Статическая генерация vs Клиентские компоненты с React элементами

**Суть проблемы:**
Next.js пытается статически сгенерировать страницы `/demo` и `/presentation` во время сборки, но эти страницы:
1. Являются клиентскими компонентами (`'use client'`)
2. Импортируют данные из `components/presentation/data.tsx`
3. Данные содержат React компоненты (иконки) как `React.ComponentType`
4. Next.js не может сериализовать React компоненты для статической генерации

---

## КОНФЛИКТУЮЩИЕ СИСТЕМЫ

### 1. **Next.js Static Generation System**
- **Что делает:** Пытается предварительно сгенерировать HTML для всех страниц
- **Где:** В процессе `next build` на этапе "Generating static pages"
- **Проблема:** Не может сериализовать React компоненты в JSON

### 2. **React Component Serialization**
- **Что делает:** Next.js пытается сериализовать пропсы и данные для передачи между сервером и клиентом
- **Где:** В `components/presentation/data.tsx` - иконки хранятся как `React.ComponentType`
- **Проблема:** React компоненты не могут быть сериализованы в JSON

### 3. **Client Component System**
- **Что делает:** Компоненты с `'use client'` должны рендериться только на клиенте
- **Где:** `app/(public)/demo/page.tsx`, `app/(public)/presentation/page.tsx`
- **Проблема:** Next.js все равно пытается их статически сгенерировать

---

## ОШИБКИ И ИХ ПРИЧИНЫ

### Ошибка 1: `Invalid revalidate value "[object Object]"`
**Файл:** `app/(public)/demo/page.tsx`
**Причина:** 
- `export const revalidate = 0` в клиентском компоненте
- Next.js интерпретирует это как объект вместо числа
- Клиентские компоненты не поддерживают `export const revalidate`

**Решение:** Убрать `export const revalidate` из клиентских компонентов

---

### Ошибка 2: `Error: Unsupported Server Component type: {...}`
**Файлы:** 
- `app/(public)/demo/page.tsx`
- `app/(public)/presentation/page.tsx`

**Причина:**
- Данные из `components/presentation/data.tsx` содержат React компоненты
- Next.js пытается сериализовать эти данные для статической генерации
- React компоненты не могут быть сериализованы в JSON

**Решение:** 
- Полностью отключить статическую генерацию для этих страниц
- Использовать `generateStaticParams = () => []` или другой подход

---

## ПРАВИЛЬНОЕ РЕШЕНИЕ

### Вариант 1: Отключить статическую генерацию через `generateStaticParams`
```typescript
export const generateStaticParams = () => []
```

### Вариант 2: Использовать динамические роуты
Переместить страницы в динамические роуты, которые не генерируются статически

### Вариант 3: Изменить структуру данных
Хранить иконки как строки (имена) и импортировать компоненты отдельно

---

## ЧТО НЕ ТАК СДЕЛАНО

1. ❌ `export const dynamic = 'force-dynamic'` в клиентских компонентах - НЕ РАБОТАЕТ
2. ❌ `export const revalidate = 0` в клиентских компонентах - вызывает ошибку
3. ❌ Layout с `export const dynamic` - конфликтует с клиентскими компонентами
4. ❌ Данные с React компонентами пытаются сериализоваться

---

## ПРАВИЛЬНЫЙ ПОДХОД

1. ✅ Убрать все `export const dynamic` и `export const revalidate` из клиентских компонентов
2. ✅ Использовать `generateStaticParams = () => []` для отключения статической генерации
3. ✅ Убедиться что layout не конфликтует
4. ✅ Проверить что данные не содержат несериализуемых элементов
