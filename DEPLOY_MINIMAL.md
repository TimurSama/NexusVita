# Минимальный деплой для тестов и демонстрации

Этот гайд поможет задеплоить первую версию **без настройки всех секретов** для быстрого тестирования и демонстрации.

## ⚡ Быстрый деплой (5 минут)

### Шаг 1: Подготовка GitHub

Если код еще не в репозитории [TimurSama/NexusVita](https://github.com/TimurSama/NexusVita):

```bash
cd "Nexus Vita/Vita Nexus"
git init
git add .
git commit -m "Initial commit - ready for deployment"
git remote add origin https://github.com/TimurSama/NexusVita.git
git branch -M main
git push -u origin main
```

**Важно:** Убедитесь, что вы в правильной директории (`Nexus Vita/Vita Nexus`), а не в корне репозитория.

### Шаг 2: Подключение Vercel

1. Зайдите на [vercel.com](https://vercel.com) и авторизуйтесь через GitHub
2. Нажмите **"Add New Project"**
3. Выберите репозиторий `TimurSama/NexusVita`
4. **Важно:** В настройках проекта укажите:
   - **Root Directory**: `Nexus Vita/Vita Nexus` (или путь к папке с `package.json`)
   - **Framework Preset**: Next.js (определится автоматически)
5. Нажмите **"Deploy"** (пока без переменных)

### Шаг 3: Минимальные переменные окружения

После первого деплоя (даже если он упадет) перейдите в **Settings** → **Environment Variables** и добавьте:

#### Обязательные (минимум для работы):

```bash
# База данных (можно использовать Vercel Postgres бесплатно)
DATABASE_URL=postgresql://...  # См. ниже как получить

# Секрет для JWT (можно сгенерировать: openssl rand -base64 32)
AUTH_SECRET=ваш-случайный-секрет-минимум-32-символа

# URL приложения (замените на ваш после первого деплоя)
NEXT_PUBLIC_APP_URL=https://your-app.vercel.app
```

#### Опциональные (для полной функциональности, можно добавить позже):

```bash
# AI (если не указано, будет использован fallback режим)
AI_PROVIDER=fallback
# OPENAI_API_KEY=... (опционально)
# ANTHROPIC_API_KEY=... (опционально)

# Cron секрет (для синхронизации данных)
CRON_SECRET=случайная-строка

# Остальные можно оставить пустыми - приложение будет работать в ограниченном режиме
```

### Шаг 4: Создание базы данных

#### Вариант A: Vercel Postgres (рекомендуется, бесплатно)

1. В Vercel Dashboard → **Storage** → **Create Database** → **Postgres**
2. Выберите бесплатный план
3. Скопируйте `DATABASE_URL` из настроек базы
4. Добавьте в Environment Variables

#### Вариант B: Supabase (бесплатно)

1. Зайдите на [supabase.com](https://supabase.com)
2. Создайте проект
3. В **Settings** → **Database** скопируйте **Connection string**
4. Добавьте в Vercel Environment Variables

#### Вариант C: Neon (бесплатно)

1. Зайдите на [neon.tech](https://neon.tech)
2. Создайте проект
3. Скопируйте connection string
4. Добавьте в Vercel Environment Variables

### Шаг 5: Миграция базы данных

После добавления `DATABASE_URL`:

```bash
# Установите Vercel CLI
npm i -g vercel

# Авторизуйтесь
vercel login

# Подключитесь к проекту
cd "Nexus Vita/Vita Nexus"
vercel link

# Скачайте переменные окружения
vercel env pull .env.local

# Выполните миграции
npx prisma migrate deploy
```

Или через Vercel Dashboard:
1. Перейдите в **Deployments**
2. Откройте последний деплой
3. В **Functions** найдите любую функцию
4. Скопируйте `DATABASE_URL` из логов
5. Выполните миграции локально с этим URL

### Шаг 6: Передеплой

1. В Vercel Dashboard → **Deployments**
2. Нажмите **"Redeploy"** на последнем деплое
3. Или сделайте новый commit в GitHub

## ✅ Что будет работать без дополнительных секретов

- ✅ Регистрация и авторизация
- ✅ Онбординг
- ✅ Главная страница и дашборд
- ✅ Все страницы интерфейса
- ✅ AI Health+ (в fallback режиме, без реального AI)
- ✅ Социальная сеть (базовые функции)
- ✅ Календарь
- ✅ Цели и достижения
- ✅ Профиль пользователя

## ⚠️ Что НЕ будет работать без секретов

- ❌ Реальные платежи (Stripe)
- ❌ Интеграции Oura/Garmin
- ❌ Реальный AI (будет fallback режим)
- ❌ KYC верификация
- ❌ Cron синхронизация (если не указан CRON_SECRET)
- ❌ Sentry мониторинг

## 🔧 Быстрая генерация секретов

```bash
# AUTH_SECRET
openssl rand -base64 32

# CRON_SECRET
openssl rand -base64 24
```

## 📝 Структура репозитория

**Важно:** Если ваш репозиторий имеет структуру с `apps/`, убедитесь, что в Vercel указан правильный **Root Directory**.

Если структура такая:
```
NexusVita/
├── apps/
│   ├── web/
│   └── api/
└── Nexus Vita/
    └── Vita Nexus/  ← ваш Next.js проект здесь
```

То в Vercel укажите **Root Directory**: `Nexus Vita/Vita Nexus`

## 🚀 После деплоя

1. Откройте ваш URL: `https://your-app.vercel.app`
2. Проверьте главную страницу
3. Попробуйте зарегистрироваться
4. Пройдите онбординг
5. Исследуйте интерфейс

## 📞 Troubleshooting

### Ошибка: "Cannot find module"
- Проверьте, что **Root Directory** указан правильно в Vercel
- Убедитесь, что `package.json` находится в корне указанной директории

### Ошибка: "Database connection failed"
- Проверьте `DATABASE_URL` в Environment Variables
- Убедитесь, что база данных доступна из интернета
- Проверьте, что миграции выполнены

### Ошибка: "Prisma Client not generated"
- Vercel должен автоматически выполнить `postinstall` скрипт
- Если нет, добавьте в Build Command: `prisma generate && next build`

---

**Готово!** Ваше приложение должно быть доступно по адресу `https://your-app.vercel.app`
