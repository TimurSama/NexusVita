# 🚀 Финальная инструкция по деплою - GitHub + Vercel

## ✅ Проверка перед деплоем

### 1. Структура проекта
- ✅ Проект находится в папке `Nexus Vita`
- ✅ `package.json` содержит все необходимые скрипты
- ✅ `vercel.json` настроен правильно
- ✅ `.gitignore` исключает `.env` файлы
- ✅ Все секреты используют переменные окружения (нет хардкода)

### 2. GitHub репозиторий
Убедитесь, что код загружен в репозиторий:
```bash
cd "Nexus Vita"
git status  # Проверьте, что все файлы закоммичены
```

Если код еще не в GitHub:
```bash
cd "Nexus Vita"
git init
git add .
git commit -m "Ready for deployment"
git remote add origin https://github.com/TimurSama/NexusVita.git
git branch -M main
git push -u origin main
```

---

## 🎯 Простой деплой в 3 шага

### Шаг 1: Подключение Vercel к GitHub (один раз)

1. Зайдите на [vercel.com](https://vercel.com) и авторизуйтесь через GitHub
2. Нажмите **"Add New Project"**
3. Выберите репозиторий `TimurSama/NexusVita`
4. **ВАЖНО:** В настройках проекта укажите:
   - **Root Directory**: `Nexus Vita`
   - **Framework Preset**: Next.js (определится автоматически)
   - **Build Command**: `prisma generate && npm run build` (уже в vercel.json)
5. Нажмите **"Deploy"** (пока без переменных окружения)

**Результат:** Первый деплой запустится, но упадет из-за отсутствия базы данных. Это нормально!

---

### Шаг 2: Настройка базы данных и переменных окружения

#### 2.1. Создайте базу данных (выберите один вариант)

**Вариант A: Vercel Postgres (рекомендуется, бесплатно)**
1. В Vercel Dashboard → **Storage** → **Create Database** → **Postgres**
2. Выберите бесплатный план
3. Скопируйте `DATABASE_URL` из настроек базы

**Вариант B: Supabase (бесплатно)**
1. Зайдите на [supabase.com](https://supabase.com)
2. Создайте проект
3. В **Settings** → **Database** скопируйте **Connection string**

**Вариант C: Neon (бесплатно)**
1. Зайдите на [neon.tech](https://neon.tech)
2. Создайте проект
3. Скопируйте connection string

#### 2.2. Добавьте переменные окружения в Vercel

После первого деплоя скопируйте URL вашего приложения (например, `https://nexus-vita.vercel.app`)

Перейдите в **Settings** → **Environment Variables** и добавьте:

**Обязательные (минимум для работы):**
```bash
DATABASE_URL=postgresql://...  # Из шага 2.1
AUTH_SECRET=ваш-случайный-секрет-минимум-32-символа
NEXT_PUBLIC_APP_URL=https://ваш-проект.vercel.app  # URL из первого деплоя
```

**Генерация секретов:**
```bash
# Windows PowerShell
[Convert]::ToBase64String((1..32 | ForEach-Object { Get-Random -Minimum 0 -Maximum 256 }))

# Linux/Mac
openssl rand -base64 32
```

**Опциональные (можно добавить позже):**
```bash
# AI (если не указано, будет fallback режим)
AI_PROVIDER=fallback

# Cron секрет (для синхронизации данных)
CRON_SECRET=случайная-строка

# Остальные можно оставить пустыми - приложение будет работать в ограниченном режиме
```

**Важно:** Выберите окружения для каждой переменной:
- ✅ **Production** — обязательно
- ✅ **Preview** — для preview деплоев (pull requests)
- ✅ **Development** — опционально

---

### Шаг 3: Применение миграций базы данных

#### Вариант A: Через Vercel CLI (рекомендуется)

```bash
# Установите Vercel CLI (если еще не установлен)
npm i -g vercel

# Авторизуйтесь
vercel login

# Перейдите в папку проекта
cd "Nexus Vita/Vita Nexus"

# Подключитесь к проекту
vercel link

# Скачайте переменные окружения
vercel env pull .env.local

# Примените схему базы данных (для первого деплоя)
npx prisma db push

# Или если у вас уже есть миграции:
# npx prisma migrate deploy
```

#### Вариант B: Прямое подключение к базе данных

Если вы знаете `DATABASE_URL`, можете применить схему напрямую:
```bash
# Установите переменную окружения
$env:DATABASE_URL="postgresql://..."  # Windows PowerShell
# или
export DATABASE_URL="postgresql://..."  # Linux/Mac

# Примените схему
npx prisma db push
```

---

### Шаг 4: Передеплой

После добавления переменных окружения и миграций:

1. В Vercel Dashboard → **Deployments**
2. Найдите последний деплой
3. Нажмите **"Redeploy"** (три точки → Redeploy)

**Или** просто сделайте новый commit в GitHub:
```bash
git commit --allow-empty -m "Trigger redeploy"
git push
```

Vercel автоматически задеплоит новую версию!

---

## ✅ Что будет работать после деплоя

- ✅ Регистрация и авторизация
- ✅ Онбординг
- ✅ Главная страница и дашборд
- ✅ Все страницы интерфейса
- ✅ AI Health+ (в fallback режиме, без реального AI)
- ✅ Социальная сеть (базовые функции)
- ✅ Календарь
- ✅ Цели и достижения
- ✅ Профиль пользователя

## ⚠️ Что НЕ будет работать без дополнительных секретов

- ❌ Реальные платежи (Stripe) - нужны `STRIPE_SECRET_KEY` и `STRIPE_WEBHOOK_SECRET`
- ❌ Интеграции Oura/Garmin - нужны соответствующие ключи
- ❌ Реальный AI - нужны `OPENAI_API_KEY` или `ANTHROPIC_API_KEY`
- ❌ KYC верификация - нужны `KYC_PROVIDER` и `KYC_API_KEY`
- ❌ Cron синхронизация - нужен `CRON_SECRET` (но cron уже настроен в vercel.json)
- ❌ Sentry мониторинг - нужен `SENTRY_DSN`

---

## 🔄 Автоматический деплой (после настройки)

После первоначальной настройки:

1. **Просто делайте commit в GitHub:**
   ```bash
   git add .
   git commit -m "Your changes"
   git push
   ```

2. **Vercel автоматически:**
   - Обнаружит изменения
   - Запустит сборку
   - Задеплоит новую версию
   - Создаст preview для pull requests

**Никаких дополнительных действий не требуется!**

---

## 🔧 Troubleshooting

### Ошибка: "Cannot find module"
- Проверьте, что **Root Directory** указан правильно: `Nexus Vita/Vita Nexus`
- Убедитесь, что `package.json` находится в корне указанной директории

### Ошибка: "Database connection failed"
- Проверьте `DATABASE_URL` в Environment Variables
- Убедитесь, что база данных доступна из интернета
- Проверьте firewall настройки базы данных

### Ошибка: "Prisma Client not generated"
- Vercel должен автоматически выполнить `postinstall` скрипт
- Проверьте, что в `package.json` есть: `"postinstall": "prisma generate"`
- Проверьте, что в `vercel.json` есть: `"buildCommand": "prisma generate && npm run build"`

### Ошибка: "Schema not found" или "Migration failed"
- Для первого деплоя используйте: `npx prisma db push`
- Если у вас уже есть миграции, используйте: `npx prisma migrate deploy`
- Убедитесь, что `DATABASE_URL` правильный и база данных доступна

### Cron jobs не работают
- Проверьте, что `CRON_SECRET` установлен в Environment Variables
- Убедитесь, что endpoint `/api/cron/sync` доступен
- Проверьте логи в Vercel Dashboard → **Deployments** → выберите деплой → **Logs**

---

## 📋 Чеклист перед первым деплоем

- [ ] Код загружен в GitHub репозиторий
- [ ] Vercel подключен к GitHub репозиторию
- [ ] Root Directory указан: `Nexus Vita/Vita Nexus`
- [ ] Создана база данных (Vercel Postgres / Supabase / Neon)
- [ ] Добавлены обязательные переменные окружения:
  - [ ] `DATABASE_URL`
  - [ ] `AUTH_SECRET` (минимум 32 символа)
  - [ ] `NEXT_PUBLIC_APP_URL` (URL после первого деплоя)
- [ ] Применены миграции базы данных
- [ ] Выполнен передеплой

---

## 🎉 Готово!

Ваше приложение будет доступно по адресу `https://ваш-проект.vercel.app`

После первоначальной настройки все последующие деплои будут автоматическими - просто делайте commit в GitHub!

---

## 📚 Дополнительные ресурсы

- [Vercel Documentation](https://vercel.com/docs)
- [Prisma Documentation](https://www.prisma.io/docs)
- [Next.js Documentation](https://nextjs.org/docs)
