# 🚀 Быстрый старт для презентации

## ⚡ Деплой за 5 минут

### Шаг 1: Подключение Vercel (2 минуты)

1. Зайдите на [vercel.com](https://vercel.com) → **Add New Project**
2. Выберите репозиторий `TimurSama/NexusVita`
3. Укажите:
   - **Root Directory**: `Nexus Vita`
   - **Framework**: Next.js (автоматически)
4. Нажмите **Deploy** (пока без переменных)

### Шаг 2: Создание базы данных (1 минута)

**Вариант A: Vercel Postgres (самый быстрый)**
1. В Vercel Dashboard → **Storage** → **Create Database** → **Postgres**
2. Выберите **Hobby** (бесплатно)
3. Скопируйте `DATABASE_URL`

**Вариант B: Supabase (альтернатива)**
1. [supabase.com](https://supabase.com) → **New Project**
2. **Settings** → **Database** → скопируйте **Connection string**

### Шаг 3: Переменные окружения (1 минута)

После первого деплоя скопируйте URL приложения (например, `https://nexus-vita.vercel.app`)

В Vercel Dashboard → **Settings** → **Environment Variables** добавьте:

```bash
# ОБЯЗАТЕЛЬНЫЕ (минимум для работы)
DATABASE_URL=postgresql://...  # Из шага 2
AUTH_SECRET=dev-secret-for-presentation-2024-change-later
NEXT_PUBLIC_APP_URL=https://ваш-проект.vercel.app  # URL после первого деплоя

# ОПЦИОНАЛЬНЫЕ (для полной функциональности, можно добавить позже)
AI_PROVIDER=fallback
CRON_SECRET=presentation-secret-2024
```

**Генерация AUTH_SECRET (PowerShell):**
```powershell
[Convert]::ToBase64String((1..32 | ForEach-Object { Get-Random -Minimum 0 -Maximum 256 }))
```

**Важно:** Выберите окружения:
- ✅ **Production**
- ✅ **Preview** (опционально)

### Шаг 4: Применение схемы БД (1 минута)

```bash
# Установите Vercel CLI (если еще не установлен)
npm i -g vercel

# Авторизуйтесь
vercel login

# Перейдите в папку проекта
cd "Nexus Vita"

# Подключитесь к проекту
vercel link

# Скачайте переменные окружения
vercel env pull .env.local

# Примените схему базы данных
npx prisma db push
```

**Или напрямую:**
```bash
# Установите DATABASE_URL
$env:DATABASE_URL="postgresql://..."  # Windows PowerShell

# Примените схему
npx prisma db push
```

### Шаг 5: Передеплой (30 секунд)

В Vercel Dashboard → **Deployments** → выберите последний деплой → **Redeploy**

**Или через Git:**
```bash
git commit --allow-empty -m "Trigger redeploy"
git push
```

---

## ✅ Что будет работать после настройки

- ✅ Регистрация и авторизация
- ✅ Онбординг
- ✅ Главная страница и дашборд
- ✅ Все страницы интерфейса
- ✅ AI Health+ (в fallback режиме)
- ✅ Социальная сеть (базовые функции)
- ✅ Календарь
- ✅ Цели и достижения
- ✅ Профиль пользователя

## ⚠️ Что НЕ будет работать (можно добавить позже)

- ❌ Реальные платежи (Stripe) - нужны ключи
- ❌ Интеграции Oura/Garmin - нужны ключи
- ❌ Реальный AI - нужны API ключи
- ❌ KYC верификация - нужны ключи

Но для презентации это не критично!

---

## 🎯 Минимальные переменные для презентации

Если нужно максимально быстро, добавьте только эти 3:

```bash
DATABASE_URL=postgresql://...
AUTH_SECRET=любая-случайная-строка-минимум-32-символа
NEXT_PUBLIC_APP_URL=https://ваш-проект.vercel.app
```

Остальное можно добавить позже!

---

## 📝 Примечания

- **Cron jobs**: Настроены на запуск 1 раз в день (совместимо с бесплатным планом Vercel)
- **База данных**: Используйте бесплатные варианты (Vercel Postgres, Supabase, Neon)
- **Fallback режимы**: Приложение работает даже без всех ключей

---

**Подробная инструкция:** [DEPLOY_FINAL.md](./DEPLOY_FINAL.md)
