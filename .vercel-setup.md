# Настройка Vercel CLI для NexusVita

## 🔐 Авторизация

Vercel не предоставляет SSH доступ (это serverless платформа), но можно использовать CLI для управления проектом и просмотра логов.

### Первоначальная настройка

```bash
# 1. Авторизация в Vercel
npm run vercel:login

# 2. Привязка проекта к Vercel
npm run vercel:link
```

## 📋 Основные команды

### Деплой
```bash
# Деплой в production
npm run vercel:deploy

# Или через npx
npx vercel --prod
```

### Просмотр логов
```bash
# Просмотр последних логов
npm run vercel:logs

# Логи в реальном времени (как tail -f)
npm run vercel:logs:follow

# Логи конкретного деплоя
npx vercel logs [deployment-url]
```

### Управление переменными окружения
```bash
# Список переменных
npm run vercel:env

# Добавить переменную
npx vercel env add VARIABLE_NAME

# Удалить переменную
npx vercel env rm VARIABLE_NAME
```

### Домены
```bash
# Список доменов
npm run vercel:domains

# Добавить домен
npx vercel domains add example.com
```

### Инспекция деплоя
```bash
# Информация о последнем деплое
npm run vercel:inspect

# Информация о конкретном деплое
npx vercel inspect [deployment-url]
```

## 🔍 Полезные команды

```bash
# Список всех деплоев
npx vercel ls

# Информация о проекте
npx vercel project ls

# Удалить деплой
npx vercel rm [deployment-url]

# Просмотр конфигурации
npx vercel project
```

## 📊 Мониторинг

### Просмотр логов в реальном времени
```bash
npm run vercel:logs:follow
```

### Фильтрация логов
```bash
# Логи только ошибок
npx vercel logs --output=raw | grep -i error

# Логи конкретного окружения
npx vercel logs --env=production
```

## ⚙️ Конфигурация проекта

Файл `vercel.json` (если нужен) должен быть в корне проекта.

## 🔗 Полезные ссылки

- [Vercel CLI Documentation](https://vercel.com/docs/cli)
- [Vercel Dashboard](https://vercel.com/dashboard)
- [Vercel API](https://vercel.com/docs/rest-api)

## ⚠️ Важно

- Vercel не предоставляет SSH доступ
- Все управление через CLI или Dashboard
- Логи доступны через CLI или Dashboard
- Переменные окружения настраиваются через CLI или Dashboard
